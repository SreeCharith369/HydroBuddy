"""Application default constants for HydroBuddy.

Runtime preferences should still be read from `data/settings.json`. These
constants provide a single reference for built-in defaults and fallback values.
"""

DEFAULT_REMINDER_INTERVAL = 60
DEFAULT_SNOOZE_INTERVAL = 10
IGNORE_TIMEOUT_SECONDS = 60
DEFAULT_DAILY_GOAL = 12
DEFAULT_RESET_TIME = "02:00"
DEFAULT_WINDOW_MARGIN = 24
DEFAULT_START_WITH_WINDOWS = True
DEFAULT_PAUSE_DURING_FULLSCREEN = True
DEFAULT_PRIMARY_MONITOR_ONLY = True
DEFAULT_SILENT_MODE = True
